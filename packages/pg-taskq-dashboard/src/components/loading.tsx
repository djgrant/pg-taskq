import React from "react";

export const fullscreen = (
  <div className="flex items-center justify-center h-screen text-gray-600">
    Loading...
  </div>
);

export const text = <span className="text-gray-600">Loading...</span>;
